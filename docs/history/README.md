# Frontend History

This directory stores archived frontend documents that are useful for traceability but are not part of the current top-level canonical doc set.

Use top-level frontend docs for current implementation truth.
Use files here for historical audits, snapshots, and prior review context.
